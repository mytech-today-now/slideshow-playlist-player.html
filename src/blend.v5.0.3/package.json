{
  "type": "module",
  "scripts": {
    "check": "node --check app.js && node --check transition-manager.js && node --check logger.js && node --check drag-sort.js && node --check supabase-config.js && node --check supabase-auth.js && node --check storage-url-resolver.js && node --check service-worker.js && node --check sw.js",
    "test:regression": "node --test tests/regression/*.test.mjs",
    "test:e2e": "playwright test",
    "sanity": "npm run check && npm run test:regression",
    "test": "npm run sanity"
  },
  "dependencies": {},
  "devDependencies": {
    "@playwright/test": "^1.61.0",
    "esbuild": "^0.25.5"
  }
}
